name: Weekly staging beta

on:
  schedule:
    # Every Sunday at 00:47 in Costa Rica (UTC-6).
    - cron: "47 6 * * 0"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: weekly-staging-beta
  cancel-in-progress: false

jobs:
  weekly-beta:
    name: Build, repack, and publish weekly beta
    runs-on: ubuntu-24.04
    timeout-minutes: 75

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set weekly release identity
        id: release
        run: |
          set -euo pipefail

          release_date=$(date -u +%F)
          branch="${GITHUB_REF_NAME}"

          echo "branch=$branch" >> "$GITHUB_OUTPUT"

          if [ "$branch" = "main" ]; then
            echo "tag=weekly-main-$release_date" >> "$GITHUB_OUTPUT"
            echo "title=Weekly Main $release_date" >> "$GITHUB_OUTPUT"
            echo "type=Main Build" >> "$GITHUB_OUTPUT"
          elif [ "$branch" = "beta" ]; then
            echo "tag=weekly-beta-$release_date" >> "$GITHUB_OUTPUT"
            echo "title=Weekly Beta $release_date" >> "$GITHUB_OUTPUT"
            echo "type=Weekly Beta" >> "$GITHUB_OUTPUT"
          else
            echo "Unsupported branch: $branch" >&2
            exit 1
          fi

      - name: Install repository-cached MIPS musl toolchain
        run: |
          set -euo pipefail
          toolchain_dir="$(scripts/install_mips_toolchain.sh "$RUNNER_TEMP")"
          echo "$toolchain_dir/bin" >> "$GITHUB_PATH"

      - name: Install build and repack tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            file genisoimage lua5.4 p7zip-full squashfs-tools zip

      - name: Build and validate player
        env:
          RELEASE_LABEL: ${{ steps.release.outputs.title }}
        run: |
          set -euo pipefail

          make -j2 target RELEASE_LABEL="$RELEASE_LABEL"
          make bootloader
          make sd_ready_test

          file open_hiby_player_target open_hiby_bootloader

          mipsel-linux-musl-readelf -h open_hiby_player_target \
            | grep 'Machine:.*MIPS' >/dev/null

          mipsel-linux-musl-readelf -h open_hiby_bootloader \
            | grep 'Machine:.*MIPS' >/dev/null

          if strings open_hiby_player_target | grep -Eq \
              'TEST BUILD|TEST_BUILD_TAG|UI_PERF_TRACE|UI_GESTURE_TRACE|UI_HITBOX_DEBUG'; then
            echo 'Diagnostic flags were found in the production player.' >&2
            exit 1
          fi

          find plugins_examples -maxdepth 1 -type f -name '*.lua' -print0 \
            | xargs -0 -r -n1 luac5.4 -p

      - name: Download verified Staging Image
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          mkdir -p base

          echo "Downloading staging image from upstream repository."
          gh release download staging-image-base \
            --pattern r1.upt \
            --dir base \
            --repo Starnished66/R1-open-source-player

          test -f base/r1.upt

          echo "Staging image downloaded successfully:"
          ls -lh base/r1.upt

      - name: Repack Staging Image
        run: |
          set -euo pipefail

          mkdir -p release

          scripts/repack_upt.sh \
            base/r1.upt \
            open_hiby_player_target \
            open_hiby_bootloader \
            release/r1.upt

          cp open_hiby_player_target release/open_hiby_player

          (
            cd plugins_examples
            zip -q -r ../release/plugin_examples.zip .
          )

          sha256sum \
            release/r1.upt \
            release/open_hiby_player \
            release/plugin_examples.zip \
            > release/SHA256SUMS

      - name: Prepare release notes
        env:
          BRANCH: ${{ steps.release.outputs.branch }}
          RELEASE_TYPE: ${{ steps.release.outputs.type }}
          RELEASE_TITLE: ${{ steps.release.outputs.title }}
        run: |
          set -euo pipefail

          {
            echo "# $RELEASE_TITLE"
            echo
            echo "**Build branch:** \`$BRANCH\`"
            echo
            echo "**Build type:** $RELEASE_TYPE"
            echo
            echo "**Commit:** [\`$GITHUB_SHA\`](https://github.com/${GITHUB_REPOSITORY}/commit/$GITHUB_SHA)"
            echo
            echo "---"
            echo

            cat WHATS_NEW.md

            echo
            echo "---"
            echo

            echo "Built automatically from commit [\`${GITHUB_SHA}\`](https://github.com/${GITHUB_REPOSITORY}/commit/$GITHUB_SHA) on branch \`$BRANCH\`."

            echo
            echo "### Downloads"
            echo

            echo "- **r1.upt** — complete firmware update."
            echo "- **open_hiby_player** — standalone Open Player update."
            echo "- **plugin_examples.zip** — all example and test plugins from the repository."

            echo
            echo "### Update only Open Player"
            echo

            echo "Copy **open_hiby_player** to **SD/.open_hiby_player/open_hiby_player**, replacing the existing file, then reboot the R1. The bootloader will use the updated player from the SD card."

            echo
            echo "Checksums are attached in **SHA256SUMS**."

          } > release/RELEASE_NOTES.md

      - name: Publish weekly release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.release.outputs.tag }}
          TITLE: ${{ steps.release.outputs.title }}
        run: |
          set -euo pipefail

          if gh release view "$TAG" >/dev/null 2>&1; then

            echo "Release $TAG already exists. Updating assets and release metadata."

            gh release upload "$TAG" \
              release/r1.upt \
              release/open_hiby_player \
              release/plugin_examples.zip \
              release/SHA256SUMS \
              --clobber

            for legacy_asset in plugins_examples.zip Test-Plugins.zip; do
              if gh release view "$TAG" --json assets --jq '.assets[].name' \
                  | grep -Fx "$legacy_asset" >/dev/null; then
                gh release delete-asset "$TAG" "$legacy_asset" --yes
              fi
            done

            gh release edit "$TAG" \
              --prerelease \
              --latest=false \
              --title "$TITLE" \
              --notes-file release/RELEASE_NOTES.md

          else

            echo "Creating new release $TAG."

            gh release create "$TAG" \
              release/r1.upt \
              release/open_hiby_player \
              release/plugin_examples.zip \
              release/SHA256SUMS \
              --latest=false \
              --prerelease \
              --target "$GITHUB_SHA" \
              --title "$TITLE" \
              --notes-file release/RELEASE_NOTES.md

          fi

          echo "Release published successfully:"
          gh release view "$TAG" \
            --json name,tagName,isPrerelease,targetCommitish,url

      - name: Preserve package as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.release.outputs.tag }}
          path: |
            release/r1.upt
            release/open_hiby_player
            release/plugin_examples.zip
            release/SHA256SUMS
            release/RELEASE_NOTES.md
          if-no-files-found: error
          retention-days: 30
